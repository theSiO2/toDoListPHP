<form method="post" action="{{ path('tasklist_delete', {'id': task_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_list.id) }}">
    <button class="btn">Delete</button>
</form>
